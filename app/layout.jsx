import './globals.css'; // Import the CSS file from the app folder

export default function Layout({ children }) {
  return (
    <html lang="en">
      <head />
      <body>{children}</body>
    </html>
  );
}
